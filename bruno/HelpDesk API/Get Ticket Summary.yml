info:
  name: Get Ticket Summary
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/tickets/:id/summary"
  params:
    - name: id
      value: t1
      type: path
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
