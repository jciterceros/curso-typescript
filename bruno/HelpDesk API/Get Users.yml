info:
  name: Get Users
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/users"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
